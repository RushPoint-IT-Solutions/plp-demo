---
name: "form-creator"
description: "Convert form screenshots to Laravel Blade forms."
applyTo:
  - "resources/views/forms/*"
  - "app/Http/Requests/*"
version: "1.0-legacy"

---

# Skill 14: Form Creator

## ðŸ— LEGACY STACK CONTEXT (CRITICAL)
- **Framework:** Laravel 5.7 ONLY (Requires PHP 7.1+ syntax).
- **Frontend:** Bootstrap 4, Vue 2, jQuery.
- **Build Tool:** Laravel Mix (`webpack.mix.js`). Run via `npm run dev`. NO Vite.

## ðŸš« Explicitly Forbidden PHP 8+ Features
- `match` expressions
- Union types (e.g., `string|int`)
- Nullsafe operator (`?->`)
- Named arguments
- Constructor property promotion
- Arrow functions (`fn() =>`)
- Typed properties
- Null-coalescing assignment (`??=`)

## ðŸ”„ Workflow
- Screenshot â†’ AI extracts fields from the provided screenshot.
- Generate a Laravel Blade form and corresponding `FormRequest` for validation.
- Insert `@csrf` and proper `old()` bindings for each field.
- Write files to `resources/views/forms/` and `app/Http/Requests/`.

## ðŸ—º Field Type Mapping
| Form Element | Blade Input Type |
|--------------|------------------|
| Text box | `text` |
| Long text | `textarea` |
| Email | `email` |
| Password | `password` |
| Dropdown | `select` |
| Date picker | `date` |
| File upload | `file` |

## ðŸ”’ Security (AUTO-INCLUDED)
- CSRF token (`@csrf`)
- Validation rules (FormRequest)
- XSS protection (`{{ }}`)

## ðŸ§° MCP Integration
- **Playwright MCP:** Analyze form screenshot.
- **Filesystem MCP:** Verify form files created.

## ðŸš¨ Escalation Protocol
- If form fields misidentified, STOP and output:
`âš ï¸ ESCALATION REQUIRED. Form field mapping error: <detail>.`

## ðŸ›‘ STOP COMMAND
Output `WAITING_FOR_HUMAN_OK` when the file is generated.
